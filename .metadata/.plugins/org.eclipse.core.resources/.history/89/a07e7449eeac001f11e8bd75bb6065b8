# Spring application name
spring.application.name=managerEmployees

# DataSource configuration for MS SQL Server
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=projects;encrypt=true;trustServerCertificate=true;
spring.datasource.username=sa
spring.datasource.password=Chitti@141
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Hibernate settings for managing database schema and logging SQL
spring.jpa.hibernate.ddl-auto=update  # This will automatically update the database schema based on entity mappings
spring.jpa.show-sql=true              # Enable SQL logging to verify the queries being executed
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect  # Dialect for MS SQL Server
spring.jpa.properties.hibernate.format_sql=true   # Format SQL queries for better readability

# Enable debug logging for Spring Framework (optional for troubleshooting)
logging.level.org.springframework=debug
